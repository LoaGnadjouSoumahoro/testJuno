import React from "react";
import a from "./style.module.css";

const Title = () => {
  return (
    <div className={`${a.title}`}>
      <h1>"Sharing the mental load is an act of love"</h1>
    </div>
  );
};

export default Title;
